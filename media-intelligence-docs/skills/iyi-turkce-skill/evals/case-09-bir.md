# Case 09 — `bir`

Input:
`Bu yaklaşım bir sistem değişikliği gerektiriyor.`

Task:
Review.

Do not automatically remove `bir`.

Ask semantic function:
- if meaning is "some kind of systemic change", `bir` may be natural;
- if broad generic category only, `Bu yaklaşım sistem değişikliği gerektiriyor` may sound compressed but can change nuance.

Expected classification: context-dependent preference, not automatic error.
